import React from "react";

const GraphPlaceholder = () => {
  return (
    <div className="graph-placeholder">
      <p>This area will later visualize the Neo4j graph.</p>
    </div>
  );
};

export default GraphPlaceholder;
